<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <ul class="nav navbar-nav">
    <% if user_signed_in? %>
      <li class="nav-item active"> <%= link_to "Log Out", destroy_user_session_path, method: :delete, :class => "nav-link" %> </li>
      <li class="nav-item active"> <%= link_to "Edit Profile", edit_user_registration_path, :class => "nav-link" %> </li>
      <li class="nav-item active"> <%= link_to "View Your Profile", user_path(current_user, :class => "nav-link") %> </li>
    <% else %>
      <li class="nav-item active"><%= link_to "Sign Up", new_user_registration_path, :class => "nav-link" %></li>
      <li class="nav-item active"><%= link_to "Log In", new_user_session_path, :class => "nav-link" %></li>
    <% end %>
  </ul>

    <% flash.each do |type, message| %>
      <div class="alert">
        <%= message %>
      </div>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>

  </body>

</html>
